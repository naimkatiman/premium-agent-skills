# demo-web

A minimal static preview of the avatar concepts. Not part of the TypeScript
build or CI typecheck.

```bash
npx serve packages/demo-web
# or
python -m http.server --directory packages/demo-web 8080
```

A full interactive demo (Lottie playback, live state toggles, a Rive embed) is
deferred past v0.1.0.
